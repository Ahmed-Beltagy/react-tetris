import left from './left';
import right from './right';
import down from './down';
import rotate from './rotate';
import space from './space';
import s from './s';
import r from './r'; // Assuming this is for reset
import p from './p'; // Assuming this is for pause

export default {
  left,
  down,
  rotate,
  right,
  space, // Keep existing mapping (Removed duplicate)
  r,     // Keep existing mapping
  p,     // Keep existing mapping
  s,     // Keep existing mapping
};
